<div class="container">
  <h2 class="title">Войдите в аккаунт</h2>
  <p class="mt">Введите логин и пароль чтобы войти на сайт</p>

  <div class="mt">
    <%= form_with scope: :session, url: session_path, data: {turbo: false} do |form| %>

      <p class="mt"><%= form.label :email %></p>
      <p><%= form.text_field :email, class: 'form-text-field' %></p>

      <p class="mt"><%= form.label :password %></p>
      <p><%= form.password_field :password, class: 'form-text-field' %></p>

      <p class="mt-lg"><%= form.submit 'Войти', class: 'button button-main' %></p>
    <% end %>  
  </div>
</div>
